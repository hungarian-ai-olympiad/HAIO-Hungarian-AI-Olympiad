# HAIO 2024 | Nyári Országos Döntő

# -- Elrejtett Kincs (CV) --
torch>=2.0
torchvision>=0.15
numpy>=1.24
matplotlib>=3.7
Pillow>=9.0
easyocr>=1.7

# -- Intelligens János Gazda (CV) --
# torch, torchvision, numpy, matplotlib, Pillow (fent)

# -- Transfer Tanulás (CV) --
# torch, torchvision, numpy, matplotlib (fent)
pandas>=2.0

# -- Spectrogram (CV) --
# torch, torchvision, numpy, matplotlib, Pillow (fent)
librosa>=0.10
timm>=0.9
requests>=2.28
tqdm>=4.65
imageio>=2.31
scikit-learn>=1.3

# -- SMS (NLP) --
# torch, numpy, pandas, scikit-learn (fent)

# -- Twitter Hangulatelemzés (NLP) --
# torch, numpy, pandas, matplotlib, scikit-learn, tqdm (fent)
torchtext>=0.15

# -- Gépi Fordítás (NLP) --
# torch, numpy, matplotlib, scikit-learn, tqdm (fent)

# -- Tű a Szénakazalban (NLP) --
# torch, numpy, matplotlib (fent)
mistralai>=0.1
